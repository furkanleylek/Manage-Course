import Login from "@/components/login/login"

export default function LoginPage() {
    return (
        <main className="flex items-center justify-center w-full h-screen bg-gradient-to-r from-[#FEAF00] to-[#F8D442] ">
            <Login />
        </main>
    )
}
